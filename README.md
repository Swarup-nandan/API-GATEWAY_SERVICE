# API-GATEWAY_SERVICE
Built a secure API Gateway &amp; Auth Service (Java, Spring Boot) with JWT authentication, refresh tokens, and Role-Based Access Control (RBAC); implemented a Redis-backed token-bucket rate limiter via atomic Lua scripts, exposing scalable, stateless REST APIs with Spring Security and JPA.
